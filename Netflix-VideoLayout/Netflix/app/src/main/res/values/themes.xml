<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Netflix" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->

        <!--  Background Color      -->
        <item name="android:windowBackground">@color/darkGray</item>

        <!-- Customize SeekBar -->
        <item name="android:seekBarStyle">@style/Widget.AppCompat.SeekBar.Discrete</item>
        <item name="android:colorControlNormal">@color/red</item> <!-- Progress Line Color -->
        <item name="android:colorControlActivated">@color/light_red</item> <!-- Secondary Progress Line Color (loading) -->

    </style>

    <style name="Theme.Netflix" parent="Base.Theme.Netflix" />
</resources>